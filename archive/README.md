# Archive Directory

This directory contains code that was removed from the active codebase but preserved for potential future use.

## Pages

- **AssetUploader.tsx** - Asset upload interface, not currently integrated into routing
- **Index.tsx** - Alternative home page with 3D model viewer, replaced by HomePage.tsx
- **ResourcesPage.tsx** - UI resources page with schema exporter, not in current navigation
- **ResetPasswordPage.tsx** - Password reset functionality, not currently integrated

## Components

Duplicate or legacy component versions that have been replaced by newer implementations.

## Notes

If you need to restore any of these files, they can be moved back to their original locations in `src/`.
